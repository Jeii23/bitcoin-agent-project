[pytest]
addopts = -q
# Include both test suites and (optionally) src-based test modules
testpaths = tests src
markers =
    integration: integration tests using known testnet XPUB vectors
filterwarnings =
    ignore::DeprecationWarning
