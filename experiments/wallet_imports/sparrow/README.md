# Sparrow Manual PSBT Imports

Place unsigned BIP-174 PSBT exports here for the wallet baseline import adapter:

- `sparrow_pct10.psbt`
- `sparrow_pct30.psbt`
- `sparrow_pct50.psbt`
- `sparrow_pct80.psbt`
- `sparrow_pct95.psbt`

The corresponding `wallet_baseline.csv` rows are disabled until these files
exist. Keep this wallet watch-only/xpub-only and do not sign or broadcast from
the baseline workflow.
